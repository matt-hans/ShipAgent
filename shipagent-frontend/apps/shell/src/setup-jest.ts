/**
 * Test setup for the shell application.
 *
 * The shell uses @angular/build:unit-test with the vitest runner.
 * Zone.js initialization is handled automatically by the Angular build.
 *
 * This file exists as the conventional test entry point and can be used
 * to add global test helpers or mock initialization.
 */

// Zone.js test environment is initialized by Angular's vitest runner.
// No manual setup required for @angular/build:unit-test with vitest.
